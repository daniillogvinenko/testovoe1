import { Header } from "widgets/Header";
import { MainPage } from "../pages/MainPage";

export const App = () => {
    return (
        <div>
            <Header />
            <MainPage />
        </div>
    );
};
